import { describe, it } from 'vitest'

describe('EnrollmentModal (Wave 0 stubs)', () => {
  it.todo('opens with employee preselected — DialogTitle contains employee.full_name')
  it.todo('switches between Lector Hikvision / Webcam / Subir JPG tabs')
  it.todo('primary CTA aria-disabled until 3 validations green')
  it.todo('closing mid-sync fires sticky Sonner toast with enrollment id and X/Y count')
  it.todo('reopening re-attaches polling to existing enrollment_id')
})
