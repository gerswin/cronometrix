import { describe, it } from 'vitest'

describe('WebcamCaptureTab (Wave 0 stubs)', () => {
  it.todo('getUserMedia called with width:640 height:480')
  it.todo('stream tracks stopped on unmount (Pitfall 6 — no lingering camera light)')
  it.todo('shows permission-denied banner with Spanish copy when DOMException thrown')
  it.todo('Capturar Rostro freezes frame to canvas and calls onCaptured with JPEG Blob')
})
