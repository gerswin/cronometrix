---
phase: 08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra
plan: 06
type: execute
wave: 6
depends_on: [08-05]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [QUALITY-GATE]
must_haves:
  truths:
    - "CLAUDE.md contains a 'Test Coverage' top-level section per D-22"
    - "CLAUDE.md Conventions block carries a Filesystem-root injection rule per D-23"
    - "Documented commands (cargo install cargo-llvm-cov, make coverage, etc.) reproduce CI exactly"
    - "Documented thresholds match the deployed values (90/85 project, 70/60 per-file)"
    - "Documented exclusion list matches the actual --ignore-filename-regex in Makefile + the actual exclude array in vitest.config.ts (no drift)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "## Test Coverage section + Conventions update for Filesystem-root injection"
      contains: "## Test Coverage"
  key_links:
    - from: "CLAUDE.md ## Test Coverage"
      to: "Makefile + scripts/enforce-coverage-floor.sh + .github/workflows/ci.yml"
      via: "documented command list mirrors actual commands"
      pattern: "make coverage"
    - from: "CLAUDE.md Conventions section"
      to: "backend/src/state/paths.rs"
      via: "documented pattern matches actual implementation"
      pattern: "state.paths."
---

<objective>
Document the test-coverage gate and the AppState path-injection convention
in `CLAUDE.md` so future Claude Code sessions and future contributors find
the rules in the canonical project-instructions surface. Per D-22
(Test Coverage section) and D-23 (Conventions block: Filesystem-root
injection rule).

Purpose: Stable documentation of the binding rules. Without this, a future
phase could regress the gate (by adding an exclusion without justification)
or regress the path pattern (by reading env vars at use-site again). The
documentation makes the constraints first-class.

Output: Two surgical edits to root `CLAUDE.md` — one new top-level section
and one extension to the Conventions block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-CONTEXT.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-RESEARCH.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-04-SUMMARY.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-05-SUMMARY.md

@CLAUDE.md
@Makefile
@scripts/enforce-coverage-floor.sh
@.github/workflows/ci.yml
@frontend/vitest.config.ts
@rust-toolchain.toml
@backend/src/state/paths.rs

<interfaces>
CLAUDE.md current structure (verified by reading the file):
- ## Project (project description + constraints)
- ## Technology Stack (extensive table)
- ## Conventions (currently a placeholder line: "Conventions not yet established. Will populate as patterns emerge during development.")
- ## Architecture (placeholder)
- ## Project Skills (placeholder)
- ## GSD Workflow Enforcement (existing)
- ## Developer Profile (placeholder)

Required additions:
1. Update Conventions section: replace placeholder with Filesystem-root injection subsection (D-23).
2. Add a new top-level "## Test Coverage" section between Conventions and Architecture.

Per D-22 the Test Coverage section MUST cover:
- Install commands (cargo-llvm-cov + nightly + llvm-tools-preview)
- Local commands (make coverage, make coverage-backend, make coverage-frontend)
- Thresholds (project + per-file)
- Exclusion policy with rationale
- HTML report locations
- CI gate (workflow file + jobs + how to read failures)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read deployed values and write the two CLAUDE.md edits in a single revision</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the actual current contents — including any GSD markers around the Conventions block; identify the exact insertion points)
    - Makefile (the canonical local commands + the actual --ignore-filename-regex value with any Plan 04 additions)
    - scripts/enforce-coverage-floor.sh (the actual threshold args 85 70 60 — confirm match D-14)
    - .github/workflows/ci.yml (the actual workflow path + job names; confirm match documentation)
    - frontend/vitest.config.ts (the actual exclude array + threshold numbers — confirm match documentation)
    - rust-toolchain.toml (the pinned nightly date — document the bump cadence)
    - backend/src/state/paths.rs (the actual env var names + defaults — document them so the convention can be verified by reading the doc)
    - .planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-04-SUMMARY.md (any exclusions that were justified — replicate the justifications in CLAUDE.md exclusion table)
    - .planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-PATTERNS.md (section "CLAUDE.md (modified)" lines 610-639 — heading style + table style guidance)
  </read_first>
  <action>
    Two surgical edits, performed in one or two adjacent Edit-tool revisions.

    BEFORE WRITING: read CLAUDE.md once, locate the exact placeholder line in the Conventions section ("Conventions not yet established. Will populate as patterns emerge during development.") and locate the heading just below Conventions (likely ## Architecture). The two insertion points are:
    - The Conventions placeholder line (gets replaced).
    - The blank line just before ## Architecture (the new ## Test Coverage section gets inserted here).

    Preserve any GSD-managed markers (e.g., HTML comments like GSD:conventions-start / GSD:conventions-end) that may surround the Conventions content. Insert the new content INSIDE those markers if present, OUTSIDE if not.

    EDIT 1 — Replace the Conventions placeholder with a Filesystem-root injection subsection (D-23):

    Replace the exact line:
        Conventions not yet established. Will populate as patterns emerge during development.

    With this content (Markdown — NOT inside a code block in CLAUDE.md):

        ### Filesystem-root injection (Phase 8)

        Code that needs a filesystem root (evidence dir, photo dir, override dir, kiosk
        capture tmp) MUST read it from `state.paths.<field>` — never via
        `std::env::var(...)` at use-site, and never via `PathBuf::from("./data/…")`.

        The `Paths` substruct on `AppState` (`backend/src/state/paths.rs`) is populated
        once at startup by `Paths::from_env()` and overridden in tests via
        `Paths::for_test(tempdir)`. This eliminates cwd-dependence (tests failing
        because they run from a different directory) and the env-var process-global
        race (parallel tests clobbering each other's env vars).

        | Path field | Env var | Default |
        |-----------|---------|---------|
        | `leaves_root` | `CRONOMETRIX_LEAVES_ROOT` | `./data/leaves` |
        | `events_root` | `CRONOMETRIX_EVENTS_ROOT` | `./data/events` |
        | `enrollments_root` | `ENROLLMENTS_DIR` | `./data/enrollments` |
        | `captures_tmp_root` | `CRONOMETRIX_CAPTURES_TMP` | `/tmp/enrollments-captures` |
        | `overrides_root` | `DATA_DIR` (joined with `overrides`) | `./data/overrides` |

        Tests must use `common::test_state_with_tmpdir(db, config)` (returns
        `(AppState, TempDir)`) and bind the returned `TempDir` to a local variable that
        outlives the test's assertions — see `backend/tests/common/mod.rs`.

    EDIT 2 — Insert a new top-level `## Test Coverage` section directly above the existing `## Architecture` heading:

    Insert this content as new lines BETWEEN the end of the Conventions block and the `## Architecture` heading. Use real Markdown headings (## and ###); do not nest under Conventions.

        ## Test Coverage

        Phase 8 established a hard-fail coverage gate. Every PR to `main` runs the
        same checks documented below and cannot merge if any threshold is missed.

        ### Install (one-time per developer)

        ```bash
        # Backend coverage tooling (cargo-llvm-cov is a tool, NOT a Cargo dependency)
        cargo install cargo-llvm-cov --locked --version 0.8.5

        # Nightly Rust is required for branch coverage (--branch is unstable on stable rustc).
        # The repo's rust-toolchain.toml pins a specific nightly date; rustup honors it
        # automatically. To install that exact toolchain explicitly:
        NIGHTLY=$(grep '^channel' rust-toolchain.toml | sed 's/.*"\(.*\)".*/\1/')
        rustup toolchain install "$NIGHTLY" --component llvm-tools-preview

        # Frontend coverage tooling is already installed
        # (vitest@4.1.5 + @vitest/coverage-v8@4.1.5 in frontend/package.json)
        cd frontend && npm ci
        ```

        ### Local commands

        ```bash
        make coverage           # Backend + frontend; both must pass
        make coverage-backend   # Backend only (cargo-llvm-cov + per-file enforcer)
        make coverage-frontend  # Frontend only (Vitest --coverage)
        ```

        The same commands run in CI (`.github/workflows/ci.yml`), so a green
        `make coverage` locally implies a green PR.

        ### Thresholds

        | Side | Scope | Lines | Branches | Functions | Statements |
        |------|-------|-------|----------|-----------|------------|
        | Backend | Project-wide | >=90% | >=85% | — | — |
        | Backend | Per file | >=70% | >=60% | — | — |
        | Frontend | Project-wide | >=90% | >=85% | >=90% | >=90% |
        | Frontend | Per file | >=70% | >=60% | >=70% | >=70% |

        Thresholds are fixed (no ratchet): the gate compares against the threshold,
        not against a stored baseline. A PR that drops coverage from 95% to 91%
        passes; from 91% to 89% fails.

        ### Exclusion policy

        Exclusions are minimal — write tests, don't shrink the denominator. Adding a
        new exclusion requires a written justification in this section. The current
        exclusions are:

        | Side | Path / regex | Justification |
        |------|--------------|---------------|
        | Backend | `main.rs` | Tokio runtime startup; not unit-testable in this phase |
        | Backend | `tests/common/*` | Test infrastructure — covering test fixtures inflates the denominator without security value |
        | Frontend | `src/components/ui/**` | Vendored shadcn copies; covered upstream |
        | Frontend | `src/app/**` | Next.js route pages; covered by E2E (out of scope for Phase 8 per CONTEXT D-10) |
        | Frontend | `src/**/*.test.{ts,tsx}` and `*.spec.{ts,tsx}` | Test files |
        | Frontend | `src/**/__tests__/**` | Test fixtures and helpers |
        | Frontend | `src/**/*.d.ts` | Type-only files; no executable code |

        Plan 04 may have added a small number of additional file-specific exclusions
        when measured coverage genuinely required them. See
        `.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-04-SUMMARY.md`
        for the case-by-case justifications. If you find yourself wanting to add a new
        exclusion, write the test instead — exclusions cap at 3 per side without an
        explicit re-discussion.

        ### HTML reports

        Local:
        - Backend: `backend/target/llvm-cov/html/index.html`
        - Frontend: `frontend/coverage/index.html`

        CI: artifacts named `backend-coverage-html` and `frontend-coverage-html` are
        attached to every workflow run (retention: 14 days). Download from the GitHub
        Actions run page even when the gate is red — the report helps drill into the
        failing file.

        ### CI gate

        Workflow file: `.github/workflows/ci.yml`

        Triggers: push to any branch, pull_request targeting `main`.

        Jobs (both required):
        - `backend-coverage` — installs nightly Rust + cargo-llvm-cov + cargo-nextest;
          runs `cargo llvm-cov nextest --branch --all-features --fail-under-lines 90`,
          then `bash scripts/enforce-coverage-floor.sh lcov.info 85 70 60`. Threshold
          miss → job exits non-zero → PR cannot merge.
        - `frontend-coverage` — installs Node 20; runs `npx vitest run --coverage`.
          Vitest enforces both project-wide and per-file thresholds natively from
          `frontend/vitest.config.ts`.

        The hard-fail behavior is locked-in (no soft-warn, no override label).
        Aligns with the audit-compliance ethos of the product (D-13).

        ### Reading a failing run

        1. Open the failing job's logs in the Actions tab.
        2. For backend: the post-processor prints `FAIL: <file> line coverage X% < floor 70%`
           (or branch). Click the file in the HTML artifact to see uncovered lines.
        3. For frontend: Vitest prints a threshold table per file; uncovered lines are
           highlighted in the HTML report.
        4. Add tests to bring the file above the floor. Don't add an exclusion unless
           the file is genuinely uncoverable in this phase.

        ### Note on private vs public repo

        HTML reports include source code excerpts. The repo is currently private, so
        artifacts are scoped to repo collaborators. If the repo ever goes public,
        revisit the artifact retention policy and consider scrubbing sensitive
        comment patterns from the HTML output.

    DEFENSIVE STEP — guard against GSD-managed Conventions sync overwriting the Filesystem-root rule:

    1. Check whether the project uses any GSD-managed Conventions sync mechanism. Run:
       ```bash
       command -v gsd-sdk >/dev/null 2>&1 && gsd-sdk query --help 2>&1 | grep -iE "conventions|sync" || echo "no gsd conventions-sync detected"
       ```
       AND
       ```bash
       grep -rE "GSD:conventions|gsd-conventions" CLAUDE.md .claude/ .planning/ 2>/dev/null | head -5 || true
       ```
       AND
       ```bash
       ls .claude/commands/ 2>/dev/null | grep -i convention || true
       ```
    2. If any of the above produces a hit (a `/gsd-conventions sync` command, GSD markers like `<!-- GSD:conventions-start -->`, or a similar mechanism), then:
       - Insert the Filesystem-root injection subsection INSIDE the GSD-managed markers if they exist, so a future `sync` operation preserves the content rather than overwriting it.
       - Add an HTML comment immediately above the Filesystem-root subsection: `<!-- Phase 8 D-23 — DO NOT remove on conventions sync; this rule is a binding code convention, not a placeholder. -->`
    3. If no GSD sync mechanism is detected, proceed with the standard Markdown insertion — but still add the protective HTML comment above the subsection so a future automated tool has a clear signal.
    4. After completing the edits, re-run any detected GSD sync command in dry-run mode (e.g., `gsd-sdk query conventions.sync --dry-run` if the verb exists) to confirm it does NOT propose to remove the new subsection. If it does, halt and surface the conflict to the user — do NOT let the gate-binding rule be silently overwritten.

    VERIFICATION AFTER WRITING:
    - The Filesystem-root injection table's env-var-and-default rows match `backend/src/state/paths.rs::Paths::from_env` exactly. Re-read `paths.rs` and the table side-by-side; if any row diverges, fix the doc.
    - The exclusion table in the Test Coverage section matches the actual `--ignore-filename-regex` in the Makefile and the actual `exclude` array in `frontend/vitest.config.ts`. If Plan 04 added file-specific exclusions, ADD ROWS to the exclusion table for each, with the justification copied from `08-04-SUMMARY.md`.
    - The threshold numbers (90/85, 70/60) match `frontend/vitest.config.ts` and `Makefile` / `scripts/enforce-coverage-floor.sh` exactly.
    - The CI workflow path + job names match `.github/workflows/ci.yml` exactly.

    Per security threat model: documenting the supply-chain pin and the negative-path documentation is part of the security control surface (RESEARCH § Security Domain). The CLAUDE.md edits make the controls discoverable without reading the source.
  </action>
  <verify>
    <automated>grep -E "^## Test Coverage" CLAUDE.md && grep -E "Filesystem-root injection" CLAUDE.md && grep -E "make coverage" CLAUDE.md && grep -E "cargo-llvm-cov" CLAUDE.md && grep -E "rust-toolchain.toml" CLAUDE.md && grep -E "CRONOMETRIX_LEAVES_ROOT" CLAUDE.md && grep -E "CRONOMETRIX_EVENTS_ROOT" CLAUDE.md && grep -E "ENROLLMENTS_DIR" CLAUDE.md && grep -E "test_state_with_tmpdir" CLAUDE.md && grep -F ".github/workflows/ci.yml" CLAUDE.md && grep -E "90%" CLAUDE.md && grep -E "85%" CLAUDE.md && grep -E "70%" CLAUDE.md && grep -E "60%" CLAUDE.md && grep -F "Phase 8 D-23 — DO NOT remove on conventions sync" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains a heading line matching `^## Test Coverage`
    - `CLAUDE.md` contains the phrase `Filesystem-root injection` somewhere in the Conventions section
    - `CLAUDE.md` contains the local-command list — at least one occurrence of `make coverage`, `make coverage-backend`, and `make coverage-frontend`
    - `CLAUDE.md` documents the install commands — contains both `cargo-llvm-cov` and `rust-toolchain.toml`
    - `CLAUDE.md` documents all five env var names — `CRONOMETRIX_LEAVES_ROOT`, `CRONOMETRIX_EVENTS_ROOT`, `ENROLLMENTS_DIR`, `CRONOMETRIX_CAPTURES_TMP`, `DATA_DIR`
    - `CLAUDE.md` documents the test-side helper — contains `test_state_with_tmpdir`
    - `CLAUDE.md` documents the CI surface — contains `.github/workflows/ci.yml`
    - `CLAUDE.md` documents the threshold numbers — contains `90%`, `85%`, `70%`, `60%`
    - The placeholder line "Conventions not yet established. Will populate as patterns emerge during development." is REMOVED — verify with `grep -F "Conventions not yet established" CLAUDE.md` returning no match
    - Markdown structural integrity: `grep -c "^## " CLAUDE.md` returns one MORE than the pre-edit value (one new top-level section added) — capture the count before editing for comparison
    - The Filesystem-root injection table is INSIDE the Conventions section (between `## Conventions` and the next `## ` heading) — verify by reading
    - The protective HTML comment `<!-- Phase 8 D-23 — DO NOT remove on conventions sync; this rule is a binding code convention, not a placeholder. -->` exists immediately above the Filesystem-root injection subsection
    - If a GSD-managed conventions sync exists, a dry-run of it does NOT propose removing the new subsection (manually re-verified by executor; documented in 08-06-SUMMARY.md)
  </acceptance_criteria>
  <done>
    CLAUDE.md is updated. Future Claude sessions and human contributors find the gate rules and the path-injection convention in the canonical project-instructions surface. The phase is now a complete, self-documenting deliverable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer | CLAUDE.md is read by every Claude Code session and serves as the canonical rules surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Repudiation | Undocumented exclusions | mitigate | The exclusion table in `## Test Coverage` is the auditable record. Adding a new exclusion without a corresponding doc update is a process violation that future plan-checkers can flag by diffing the Makefile / vitest.config.ts against the doc. |
| T-08-20 | Information Disclosure | Public artifact disclosure | accept (with note) | The doc explicitly notes the private-vs-public repo distinction so a future visibility change doesn't silently expose source. The mitigation is operational, not technical, but the doc makes it discoverable. |
| T-08-21 | Tampering | Convention drift (env-var-at-use-site regression) | mitigate | The Filesystem-root injection rule in Conventions is the canonical reference for future contributors. A future PR that re-introduces `env::var(...LEAVES_ROOT...)` at use-site is a documented violation, catchable by `grep` in plan-check or human review. |
</threat_model>

<verification>
1. CLAUDE.md contains `## Test Coverage` with all required subsections (install, local commands, thresholds, exclusions, HTML reports, CI gate, reading failures, public-vs-private note)
2. CLAUDE.md Conventions section carries the Filesystem-root injection rule with the env-var-and-default table
3. The placeholder line "Conventions not yet established..." is removed
4. All grep-based acceptance criteria pass
5. Documented values match deployed values exactly (env vars match paths.rs; thresholds match vitest.config.ts and Makefile; CI path matches the actual workflow)
</verification>

<success_criteria>
- Two surgical edits to CLAUDE.md committed
- D-22 satisfied (Test Coverage section)
- D-23 satisfied (Conventions § Filesystem-root injection)
- All grep-based acceptance criteria pass
- Documentation matches deployed configuration verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-06-SUMMARY.md` confirming:
- The exact line where the Filesystem-root injection subsection now lives in CLAUDE.md
- The exact line where the ## Test Coverage section now lives in CLAUDE.md
- A note that the phase is complete: 5 deliverables landed (Plans 01-06) and the binding rules are documented.
</output>
