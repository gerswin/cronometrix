---
phase: 11-v1-0-live-environment-validation
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md
  - .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md
  - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-licensing-deployment/06-VERIFICATION.md
autonomous: true
requirements:
  - VALIDATE-LIC-05-CLONE
  - VALIDATE-HIKVISION-LIVE

must_haves:
  truths:
    - "evidence/06-lic-05-deferral/README.md captures D-06/D-07 risk-acceptance with the exact pre-flight checklist a deploy engineer must run on first install (machine A activate → snapshot fingerprint → relocate JWT to machine B → confirm reject)"
    - "evidence/07-hikvision-deferral/README.md captures D-10 risk-acceptance citing mock_hikvision sufficiency for v1.0 + first-install field test contract"
    - "REQUIREMENTS.md ## v1.1 Backlog has a new row `LIC-05-CROSS-HOST` alongside the existing `DEPL-03-AUTO` (D-09)"
    - "06-VERIFICATION.md deferred-items table cross-references Phase 11 LIC-05 deferral evidence (D-08)"
    - "Both README files have Verdict: DEFERRED (not PASS, not FAIL — a third value documented in 00-overview's schema row)"
  artifacts:
    - path: ".planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md"
      provides: "Risk-accept doc for LIC-05 cross-host clone test deferral"
      contains: "Verdict: DEFERRED"
      min_lines: 60
    - path: ".planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md"
      provides: "Risk-accept doc for real Hikvision alertStream deferral"
      contains: "Verdict: DEFERRED"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.1 Backlog with new LIC-05-CROSS-HOST row"
      contains: "LIC-05-CROSS-HOST"
    - path: ".planning/phases/06-licensing-deployment/06-VERIFICATION.md"
      provides: "Deferred-items table now cross-links Phase 11 LIC-05 evidence (D-08)"
      contains: ".planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md"
  key_links:
    - from: "evidence/06-lic-05-deferral/README.md"
      to: "REQUIREMENTS.md v1.1 Backlog"
      via: "External refs section linking to LIC-05-CROSS-HOST row"
      pattern: "LIC-05-CROSS-HOST"
    - from: "06-VERIFICATION.md deferred-items table"
      to: "evidence/06-lic-05-deferral/README.md"
      via: "Markdown link with relative path from 06 dir to 11 dir"
      pattern: "11-v1-0-live-environment-validation/evidence/06-lic-05-deferral"
    - from: "evidence/07-hikvision-deferral/README.md"
      to: "backend/tests/license_bypass_safety.rs + mock_hikvision.rs"
      via: "Cite mock-hikvision integration tests as the v1 confidence floor"
      pattern: "mock_hikvision\\.rs|license_bypass_safety\\.rs"
---

<objective>
Capture the formal risk-accept docs for the two Phase 11 evidence items that will NOT be physically run in this phase: LIC-05 cross-host clone-rejection (deferred to first prod install per D-06) and real Hikvision alertStream end-to-end (out of scope per D-10). Also extend REQUIREMENTS.md ## v1.1 Backlog with `LIC-05-CROSS-HOST` (per D-09) and patch 06-VERIFICATION.md's deferred-items table to cross-reference the Phase 11 LIC-05 evidence file (per D-08), preserving bidirectional audit traceability.

Purpose: An audit verdict of "we didn't run this test in CI" must still be evidenced — otherwise the milestone has a silent gap. Phase 11 closes both deferrals with `Verdict: DEFERRED` documents that future deploy engineers (LIC-05) and field test operators (Hikvision) can read at first prod install to know exactly what manual validation needs to happen.

Output: 2 risk-accept README files, 1 backlog row added to REQUIREMENTS.md, 1 cross-reference added to 06-VERIFICATION.md, overview index updated. Pure markdown — fully autonomous, no operator action required, no external infrastructure touched.

This plan is AUTONOMOUS: the executor reads CONTEXT.md decisions D-06..D-10 + the existing 06-VERIFICATION.md deferred table, writes the two risk-accept docs, edits the two existing markdown files, and commits. No human checkpoint needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-licensing-deployment/06-VERIFICATION.md
@.planning/phases/06-licensing-deployment/06-04-SUMMARY.md
@backend/src/license/fingerprint.rs
@backend/src/license/service.rs
@backend/tests/license_bypass_safety.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write LIC-05 cross-host clone-rejection deferral evidence + Hikvision deferral evidence</name>
  <read_first>
    - .planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md (D-06, D-07, D-08, D-09, D-10 — verbatim required text)
    - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md (must already exist from 11-01)
    - .planning/phases/06-licensing-deployment/06-VERIFICATION.md (sections — confirm there is a deferred-items table and find the LIC-05 row)
    - backend/src/license/fingerprint.rs (read just the public function names + the formula comment so the README accurately describes the fingerprint inputs)
    - backend/src/license/service.rs (read the bypass + activation evaluation function names so the README accurately names the in-process tests)
  </read_first>
  <files>
    - .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md
    - .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md
  </files>
  <action>
    Step 1 — Write `evidence/06-lic-05-deferral/README.md` with the exact content below. Replace `{today's ISO date}` with `date +%Y-%m-%d`. Replace `{operator}` with the planner's git user.email or the literal string `Phase 11 planner` if unknown.

    ```markdown
    # Evidence: LIC-05 Cross-Host Clone-Rejection — Deferral to First Production Install

    - **Date captured:** {today's ISO date}
    - **Captured by:** {operator}
    - **Command run / action:** No live test executed. This document records the formal deferral of the LIC-05 cross-host clone-rejection test to the first paying-customer production install per Phase 11 D-06 / D-07 / D-09.
    - **Expected:** A live cross-host clone test would: (1) install Cronometrix on machine A, (2) activate license + capture hardware fingerprint, (3) copy the cached license JWT to machine B, (4) start Cronometrix on machine B, (5) confirm activation REJECTS with `AppError::Unlicensed` because the fingerprint computed on machine B does not match the JWT's bound fingerprint.
    - **Actual:** Test not physically executed in Phase 11. The code path that LIC-05 protects is exercised by:
      - `backend/tests/license_*` (in-process synthetic mismatch tests — covered in 06-04 + 08-04B)
      - `backend/tests/license_bypass_safety.rs::bypass_without_e2e_aborts_with_code_2` (locks the fail-closed contract)

      Live cross-host hardware proof is deferred to first production deployment because: (a) it requires two distinct physical machines or two cloud VMs, (b) the customer's deploy environment IS the validation environment, (c) the fingerprint formula (CPU + MAC + disk serial — see `backend/src/license/fingerprint.rs`) is platform-specific and worth verifying on the actual customer host class rather than synthetic CI hardware.

    - **Verdict:** DEFERRED — to first production install per Phase 11 D-06.

    ## Risk Acknowledgement

    LIC-05 is the highest-risk deferral in this milestone. A license-binding failure at customer deploy would be visible (the activation literally would not succeed) but would also be embarrassing — it surfaces a v1.0 protection gap exactly when the system is supposed to be paid-for-and-operational. Phase 11 accepts this risk because:

    1. The in-process tests cover the AppError::Unlicensed branch (synthetic mismatch).
    2. `backend/tests/license_bypass_safety.rs` proves the misconfiguration abort contract (`CRONOMETRIX_LICENSE_BYPASS=true` without `CRONOMETRIX_E2E=true` exits 2). This eliminates the worst-case footgun of a deploy script silently disabling the gate.
    3. First customer install is the appropriate moment for the live test — that's when real hardware enters the loop.
    4. v1.1 backlog item `LIC-05-CROSS-HOST` (added to REQUIREMENTS.md by this plan) carries the formal commitment to convert this deferral into a CI-runnable test using two cloud VMs.

    ## Pre-Flight Checklist for First Production Install (mandatory)

    The deploy engineer running the first paid customer install MUST run this checklist BEFORE handing the system over to the customer. Treat it as the live LIC-05 evidence run.

    ```
    # On machine A (the customer's actual server):
    1. Run installer: curl https://install.cronometrix.com/{client-slug} | bash
    2. On the /setup/license screen, paste the customer-issued license key.
    3. Confirm activation succeeds: GET /api/v1/license/status returns valid=true.
    4. SSH in and snapshot the fingerprint:
       cat /var/lib/cronometrix/license.jwt > /tmp/license-machineA.jwt
       curl localhost/api/v1/license/fingerprint > /tmp/fingerprint-machineA.json
    5. Save both files to a secure location (NOT in the customer's repo).

    # On machine B (a separate physical machine — laptop, second VM, anything different):
    6. Run installer same way: curl ... | bash
    7. SCP the JWT from step 4 to machine B:
       scp /tmp/license-machineA.jwt machineB:/var/lib/cronometrix/license.jwt
    8. Restart cronometrix-api service.
    9. Curl /api/v1/license/status — MUST return 401 Unlicensed (or whatever AppError::Unlicensed maps to).
    10. Curl /api/v1/license/fingerprint — fingerprint MUST differ from the value in fingerprint-machineA.json.
    11. Cleanup: stop machineB cronometrix service, delete the relocated JWT.

    PASS criteria: step 9 returns 401 Unlicensed AND step 10 shows a different fingerprint.
    FAIL criteria: step 9 returns 200 OK (clone succeeded — LIC-05 is broken).
    ```

    The deploy engineer should commit a screenshot or log of step 9's response to a (separate) field-test evidence dir on first install, and update v1.1 backlog item `LIC-05-CROSS-HOST` with the result.

    - **Artifacts:** None — this is a markdown-only deferral evidence.
    - **External refs:**
      - REQUIREMENTS.md `## v1.1 Backlog` row `LIC-05-CROSS-HOST` (added by Plan 11-03 per D-09)
      - `.planning/phases/06-licensing-deployment/06-VERIFICATION.md` deferred-items table (cross-reference added by Plan 11-03 per D-08)
      - `backend/src/license/fingerprint.rs` (the fingerprint formula under live test)
      - `backend/tests/license_bypass_safety.rs::bypass_without_e2e_aborts_with_code_2` (related fail-closed contract test)
      - Phase 11 11-CONTEXT.md decisions D-06, D-07, D-09 (the binding deferral decisions)
    ```

    Step 2 — Write `evidence/07-hikvision-deferral/README.md` with this content:

    ```markdown
    # Evidence: Real Hikvision alertStream Live Test — Deferral to First Production Install

    - **Date captured:** {today's ISO date}
    - **Captured by:** {operator}
    - **Command run / action:** No live test executed. This document records the formal deferral of the real-hardware Hikvision alertStream end-to-end test per Phase 11 D-10.
    - **Expected:** A live test would: (1) connect a real Hikvision DS-K1T341 or DS-K1T342 unit to the network, (2) register it via /api/v1/devices, (3) trigger a face-recognition event on the device, (4) confirm the event lands in attendance_events with correct UTC timestamp + employee mapping + dedup behavior (30-second window per EVT-03).
    - **Actual:** Test not executed in Phase 11. The integration is sufficiently exercised by:
      - `backend/src/bin/mock_hikvision.rs` (Phase 9 — impersonates a real Hikvision unit including digest auth + alertStream multipart XML protocol)
      - `frontend/e2e/devices.spec.ts` (Phase 9 — drives the full registration → command-dispatch → audit assertion path against the mock)
      - Phase 2 unit + integration tests covering the alertStream listener, supervisor reconnect loop, and event deduplication

      Real-hardware live test is deferred to first production install because: (a) it requires physical Hikvision hardware in the test environment, (b) the customer's hardware IS the validation environment, (c) ISAPI XML schema variations between Hikvision device models (DS-K1T341 vs DS-K1T342, per Phase 2 blocker note in STATE.md) make synthetic CI hardware an unreliable proxy.

    - **Verdict:** DEFERRED — out of Phase 11 scope per D-10. First production install is the live evidence opportunity.

    ## Risk Acknowledgement

    EVT-01/02 risks at first install: a real Hikvision device that uses an ISAPI dialect the mock did not anticipate could fail to establish alertStream, fail digest auth, or produce events the parser does not handle. Mitigations:

    1. Phase 2 STATE.md blocker note already flags ISAPI XML schema variation between models — first deploy must capture real alertStream traffic with `tcpdump` or equivalent before declaring EVT-01 green at the customer.
    2. Phase 9's `mock_hikvision.rs` is the canonical contract — if the real device produces a payload it does not handle, that's a real-world adapter gap to fix in v1.x, not a Phase 11 PASS gate.
    3. v1.1 backlog has the documented opportunity to convert this deferral into a CI-runnable test using a Hikvision-on-loan or virtualized firmware once available.

    ## Field Test Checklist for First Production Install

    Suggested (not mandatory like LIC-05's checklist) on first deploy with real Hikvision hardware:

    1. Register device via UI (Devices → Add) with real IP + ISAPI credentials + traffic direction.
    2. Watch backend logs for `alertStream connected to {ip}:{port}`.
    3. Trigger a face event on the device (have someone approach the camera).
    4. Confirm event appears in /api/v1/events within 5 seconds.
    5. Trigger a second event from the same employee within 30 seconds — confirm dedup (only 1 row in attendance_events).
    6. Disconnect device network for 60s, reconnect — confirm supervisor loop reconnects (per EVT-02).

    PASS criteria: all 6 steps green.
    FAIL criteria: any step red → file as v1.x adapter bug, NOT as Phase 11 retroactive failure.

    - **Artifacts:** None — markdown-only deferral evidence.
    - **External refs:**
      - `backend/src/bin/mock_hikvision.rs` (the v1 confidence floor)
      - `frontend/e2e/devices.spec.ts` (E2E coverage of the full path against mock)
      - Phase 2 STATE.md blocker note on ISAPI XML schema variation
      - Phase 11 11-CONTEXT.md D-10 (the binding deferral decision)
    ```

    Step 3 — Both files committed in Task 3 below.
  </action>
  <verify>
    <automated>cd /Users/gerswin/Proyectos/cronometrix && grep -q '^- \*\*Verdict:\*\* DEFERRED' .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md && grep -q '^- \*\*Verdict:\*\* DEFERRED' .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md && grep -q 'Pre-Flight Checklist for First Production Install' .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md && grep -q 'mock_hikvision' .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md && grep -q 'license_bypass_safety' .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md && wc -l .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md | awk '$1 >= 60 {exit 0} {exit 1}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/06-lic-05-deferral/README.md` exists, has `Verdict: DEFERRED`, includes the 11-step pre-flight checklist (greppable: `Pre-Flight Checklist for First Production Install`), references both `license_bypass_safety` and `fingerprint.rs`, and is at least 60 lines
    - `evidence/07-hikvision-deferral/README.md` exists, has `Verdict: DEFERRED`, references `mock_hikvision` and `devices.spec.ts`, includes a 6-step field test checklist
    - Both READMEs have all 8 D-02 schema sections (Date captured, Captured by, Command run, Expected, Actual, Verdict, Artifacts, External refs)
  </acceptance_criteria>
  <done>Two risk-accept docs written, schema-conformant, with explicit field-test checklists for the deploy engineer.</done>
</task>

<task type="auto">
  <name>Task 2: Add LIC-05-CROSS-HOST to REQUIREMENTS.md v1.1 Backlog and cross-reference 06-VERIFICATION.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 227-234 — current v1.1 Backlog table; confirm DEPL-03-AUTO row format so new row matches)
    - .planning/phases/06-licensing-deployment/06-VERIFICATION.md (find the deferred-items table and the LIC-05 row — confirm exact format before adding cross-reference)
    - .planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md (D-08, D-09 — exact required text)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/phases/06-licensing-deployment/06-VERIFICATION.md
  </files>
  <action>
    Step 1 — Append a new row to `.planning/REQUIREMENTS.md` `## v1.1 Backlog` table. The current table has 1 row (DEPL-03-AUTO). Add the LIC-05-CROSS-HOST row IMMEDIATELY AFTER it (do not reorder existing rows; do not modify the DEPL-03-AUTO row). Use the literal D-09 wording extended for clarity:

    ```markdown
    | LIC-05-CROSS-HOST | Real-hardware cross-host clone test on 2 distinct machines or 2 cloud VMs to prove LIC-05's anti-cloning binding. | Currently deferred to first production deployment evidence — see `.planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md`. v1.1 should evaluate whether this can be CI-automated using two ephemeral cloud VMs (DigitalOcean / Hetzner) or whether it remains a customer-deploy verification step. Source: deferred from Phase 11 D-06/D-07/D-09. |
    ```

    Use the Edit tool to insert this row. Locate the existing line `| DEPL-03-AUTO | Installer auto-registers...` (around line 233) and append the new row on the next line. Do not insert blank lines between rows. After insertion, the table will have 2 rows.

    Step 2 — Update `.planning/REQUIREMENTS.md` "Last updated" footer (currently at the bottom of the file): change the date stamp from `2026-04-29 — traceability refreshed for milestone v1.0 sign-off (Phase 10-03)` to `{today's ISO date} — Phase 11 added LIC-05-CROSS-HOST to v1.1 Backlog (Plan 11-03)`. Use the Edit tool with the exact `*Last updated:` prefix as anchor.

    Step 3 — Update `.planning/phases/06-licensing-deployment/06-VERIFICATION.md`. Read it first to locate the deferred-items table and the LIC-05 row. Per D-08, add a cross-reference column entry pointing to `.planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md`. Two acceptable shapes (pick whichever matches the existing table's style):

    Shape A — if the deferred-items table already has a "Notes" or "Cross-ref" column, add the relative-path link to that cell on the LIC-05 row.

    Shape B — if no such column exists, append a paragraph IMMEDIATELY AFTER the deferred-items table:
    ```markdown
    > **Phase 11 deferral cross-reference (D-08):** The LIC-05 row above is now formally backed by Phase 11 evidence. The risk-acceptance rationale, the v1.1 backlog commitment (`LIC-05-CROSS-HOST`), and the mandatory pre-flight checklist for the deploy engineer at first production install are recorded in [`Phase 11 / 06-lic-05-deferral`](../11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md).
    ```

    Use Read first to determine the table's actual column structure, then pick A or B accordingly. Do NOT modify any other rows or sections of 06-VERIFICATION.md.
  </action>
  <verify>
    <automated>cd /Users/gerswin/Proyectos/cronometrix && grep -q '| LIC-05-CROSS-HOST |' .planning/REQUIREMENTS.md && grep -q 'evidence/06-lic-05-deferral/README\.md' .planning/REQUIREMENTS.md && grep -q '11-v1-0-live-environment-validation/evidence/06-lic-05-deferral' .planning/phases/06-licensing-deployment/06-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` v1.1 Backlog table has 2 rows total (DEPL-03-AUTO unchanged + new LIC-05-CROSS-HOST row)
    - LIC-05-CROSS-HOST row references `evidence/06-lic-05-deferral/README.md` (greppable)
    - REQUIREMENTS.md footer "Last updated" date reflects Phase 11 edit
    - `06-VERIFICATION.md` contains a path string `11-v1-0-live-environment-validation/evidence/06-lic-05-deferral` (greppable)
    - DEPL-03-AUTO row in v1.1 Backlog is byte-identical to its pre-edit state (verify with `git diff .planning/REQUIREMENTS.md` showing the change is purely additive)
  </acceptance_criteria>
  <done>v1.1 backlog has the LIC-05-CROSS-HOST commitment; 06-VERIFICATION.md cross-references the Phase 11 evidence; bidirectional traceability is sealed.</done>
</task>

<task type="auto">
  <name>Task 3: Update overview index and commit atomically</name>
  <read_first>
    - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md (must already exist from 11-01; rows 06 + 07 currently say `pending`)
    - All 4 files modified by Tasks 1+2 (verify they exist with correct content)
  </read_first>
  <files>
    - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md
  </files>
  <action>
    Step 1 — Edit `evidence/00-overview.md`: change the row 06 verdict from `[pending](./06-lic-05-deferral/README.md)` to `[DEFERRED](./06-lic-05-deferral/README.md)` and row 07 verdict from `[pending](./07-hikvision-deferral/README.md)` to `[DEFERRED](./07-hikvision-deferral/README.md)`. Bump the "Last updated" line to today's ISO date.

    Step 2 — Update the overview's evidence schema bullet "Verdict (`PASS` | `FAIL` | `DEFERRED`)" — already in 11-01's template. Verify it includes DEFERRED; if not, add it. (11-01's template DOES include DEFERRED, so this should be a no-op.)

    Step 3 — Commit atomically (one commit per D-14):

    ```
    git add .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/README.md \
            .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/README.md \
            .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md \
            .planning/REQUIREMENTS.md \
            .planning/phases/06-licensing-deployment/06-VERIFICATION.md
    git commit -m "docs(11-03): LIC-05 + Hikvision deferral evidence + v1.1 Backlog + 06-VERIFICATION xref"
    ```

    All 5 file modifications land in one commit so the audit trail is atomic — readers can git-blame to the single commit and see the full deferral chain (evidence + backlog + cross-ref) without hunting through multiple commits.
  </action>
  <verify>
    <automated>cd /Users/gerswin/Proyectos/cronometrix && grep -q '\[DEFERRED\](\./06-lic-05-deferral/README\.md)' .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md && grep -q '\[DEFERRED\](\./07-hikvision-deferral/README\.md)' .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md && git log -1 --pretty=%s | grep -q 'docs(11-03):' && git log -1 --name-only --pretty=format: | sort | tr '\n' ' ' | grep -q '06-VERIFICATION' && git log -1 --name-only --pretty=format: | sort | tr '\n' ' ' | grep -q 'REQUIREMENTS.md' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/00-overview.md` rows 06 and 07 verdicts now show `DEFERRED` (not `pending`)
    - Latest commit message starts with `docs(11-03):` and touches all 5 expected paths (verifiable: `git log -1 --name-only` lists the 5 files)
    - The commit contains BOTH evidence READMEs AND the REQUIREMENTS.md backlog edit AND the 06-VERIFICATION.md cross-reference (single atomic commit)
    - `du -sh .planning/phases/11-v1-0-live-environment-validation/evidence/` total stays under 150 MB (markdown only — should be well under 1 MB delta)
  </acceptance_criteria>
  <done>Plan 11-03 evidence fully committed; rows 06 and 07 in overview transition `pending → DEFERRED`; v1.1 Backlog now records the LIC-05-CROSS-HOST commitment; 06-VERIFICATION.md cross-links Phase 11 evidence per D-08. Audit traceability is bidirectional and atomic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo internal | All edits are markdown-only; no external trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Repudiation | Future deploy engineer skips the LIC-05 pre-flight checklist on first install | mitigate | The README explicitly labels the checklist as "mandatory" and lists PASS/FAIL criteria; CLAUDE.md should later be updated (out of Phase 11 scope) to point at this evidence file from a deploy-prep section |
| T-11-09 | Repudiation | Reader assumes "DEFERRED" means "skipped without justification" | mitigate | Both READMEs include a "Risk Acknowledgement" section explaining the choice + naming the in-process tests + naming the v1.1 backlog item. The verdict is documented and traceable, not silent |
| T-11-10 | Tampering | Future edit accidentally drops the LIC-05-CROSS-HOST row from v1.1 Backlog | accept | Git history is the immutability mechanism. Phase 11 commits this row atomically with the evidence README so any future deletion is reviewable |

</threat_model>

<verification>
- 2 risk-accept evidence READMEs exist with `Verdict: DEFERRED` and full D-02 schema
- LIC-05 README has the 11-step pre-flight checklist (greppable: `step 9 returns 401 Unlicensed`)
- Hikvision README has the 6-step field test checklist
- `.planning/REQUIREMENTS.md` v1.1 Backlog has `LIC-05-CROSS-HOST` row alongside `DEPL-03-AUTO`
- `.planning/phases/06-licensing-deployment/06-VERIFICATION.md` cross-references the Phase 11 LIC-05 evidence file
- `evidence/00-overview.md` rows 06 + 07 = `DEFERRED`
- Single atomic commit `docs(11-03):` touches all 5 files
- No source code changes (markdown-only)
</verification>

<success_criteria>
- LIC-05 cross-host deferral fully documented with operator pre-flight checklist (per D-06/D-07)
- Hikvision live-test deferral fully documented with field test checklist (per D-10)
- v1.1 Backlog now carries `LIC-05-CROSS-HOST` (per D-09)
- 06-VERIFICATION.md cross-references Phase 11 LIC-05 evidence (per D-08)
- Bidirectional audit traceability: 06-VERIFICATION → Phase 11 evidence → REQUIREMENTS.md v1.1 Backlog → back to evidence
- VALIDATE-LIC-05-CLONE + VALIDATE-HIKVISION-LIVE both have committed evidence (Verdict: DEFERRED is the gate-pass for these two requirements per D-13)
- No production code touched (D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-0-live-environment-validation/11-03-SUMMARY.md` summarizing: which D-XX decisions were honored, exact line numbers added to REQUIREMENTS.md and 06-VERIFICATION.md, and a copy of the LIC-05 pre-flight checklist (so the next deploy-prep doc can reference it without reopening the evidence README).
</output>
