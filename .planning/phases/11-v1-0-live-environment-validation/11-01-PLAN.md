---
phase: 11-v1-0-live-environment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/README.md
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/backend-coverage-job.txt
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/frontend-coverage-job.txt
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/e2e-tests-job.txt
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/backend-coverage-html.tar.gz
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/frontend-coverage-html.tar.gz
  - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/playwright-report.tar.gz
  - .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/README.md
  - .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/make-e2e.txt
  - .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/playwright-report.tar.gz
autonomous: false
requirements:
  - VALIDATE-CI-GREEN
user_setup:
  - service: github-actions
    why: "Must trigger live CI run on real GitHub Actions runner"
    env_vars: []
    dashboard_config:
      - task: "Push working branch and observe Actions tab"
        location: "https://github.com/{org}/cronometrix/actions"

must_haves:
  truths:
    - ".gitattributes contains '*.html linguist-generated=true' rule before HTML evidence is committed"
    - "evidence/00-overview.md indexes all 5 evidence items with their plan IDs and current verdicts"
    - "evidence/01-ci-green/README.md has Verdict: PASS with the GitHub Actions run URL recorded"
    - "evidence/01-ci-green captures terminal output for all 3 CI jobs (Backend Coverage, Frontend Coverage, E2E Tests)"
    - "evidence/02-local-make-e2e/README.md has Verdict: PASS with `make e2e` exit code 0 and all 72 specs passing"
  artifacts:
    - path: ".gitattributes"
      provides: "linguist-generated marker for HTML evidence so language stats are not polluted"
      contains: "*.html linguist-generated=true"
    - path: ".planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md"
      provides: "Index of all Phase 11 evidence items with verdict status"
      min_lines: 25
    - path: ".planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/README.md"
      provides: "Verdict-bearing report for live CI green run"
      contains: "Verdict: PASS"
    - path: ".planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/README.md"
      provides: "Verdict-bearing report for local make e2e run"
      contains: "Verdict: PASS"
  key_links:
    - from: "evidence/01-ci-green/README.md"
      to: "GitHub Actions run URL"
      via: "External refs section per D-02 schema"
      pattern: "https://github.com/.*/actions/runs/[0-9]+"
    - from: "evidence/00-overview.md"
      to: "evidence/01-ci-green/README.md"
      via: "Markdown link with relative path per D-05"
      pattern: "\\./01-ci-green/README\\.md"
    - from: "evidence/00-overview.md"
      to: "evidence/02-local-make-e2e/README.md"
      via: "Markdown link with relative path per D-05"
      pattern: "\\./02-local-make-e2e/README\\.md"
---

<objective>
Capture committed evidence that the v1.0 CI gates (`Backend Coverage` + `Frontend Coverage` + `E2E Tests`) pass green on a real GitHub Actions runner AND that `make e2e` passes locally against the real dev stack. Also lay down the shared Phase 11 repo plumbing (`.gitattributes` rule + `evidence/00-overview.md` index) that all later plans extend.

Purpose: Phase 8 Plan 05 + Phase 9 Plan 12 deferred their live-runner validation; this plan closes both deferrals with auditable artifacts (GH Actions URLs + downloaded HTML reports + raw job logs) so the milestone v1.0 sign-off has hard proof — not just the static-yaml verification done at merge time.

Output: `.gitattributes` updated, `evidence/00-overview.md` index page, `evidence/01-ci-green/` with 3 job-log captures + 3 HTML report tarballs + verdict README, `evidence/02-local-make-e2e/` with terminal log + Playwright HTML report tarball + verdict README.

This plan is NOT autonomous: an operator must (1) push the working branch and trigger CI, (2) wait for the runner, (3) download artifacts and paste back the run URL + status, (4) run `make e2e` locally and paste back the exit code + log. The executor's role is to scaffold dirs, generate the operator checklist, validate pasted evidence, and write the verdict READMEs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-05-SUMMARY.md
@.planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-12-SUMMARY.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo prep — add .gitattributes rule and scaffold evidence directory with overview index</name>
  <read_first>
    - .planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md (D-01 through D-05)
    - .gitignore (verify no rule excludes `evidence/` or `*.tar.gz` under `.planning/`)
    - existing root files in repo (use `ls -1 /Users/gerswin/Proyectos/cronometrix/` to confirm `.gitattributes` does not yet exist)
  </read_first>
  <files>
    - .gitattributes
    - .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md
    - .planning/phases/11-v1-0-live-environment-validation/evidence/01-ci-green/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/03-ci-red/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/04-branch-protection/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/05-installer-smoke/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/06-lic-05-deferral/.gitkeep
    - .planning/phases/11-v1-0-live-environment-validation/evidence/07-hikvision-deferral/.gitkeep
  </files>
  <action>
    Step 1 — Create `.gitattributes` at repo root with EXACTLY this content (no extra rules, this is the entire file Phase 11 needs):

    ```
    # HTML reports committed under .planning/phases/11-*/evidence/ are downloaded
    # CI artifacts (Playwright + cargo-llvm-cov + Vitest reports). Mark them as
    # linguist-generated so GitHub web UI excludes them from per-language stats
    # (per Phase 11 D-04).
    *.html linguist-generated=true
    ```

    If `.gitattributes` already exists at repo root (verify with `ls /Users/gerswin/Proyectos/cronometrix/.gitattributes` before writing), append the rule to the END of the existing file rather than overwriting. Do not duplicate the rule.

    Step 2 — Create the 7 evidence subdirectories. Each one gets an empty `.gitkeep` file so git tracks the directory shell before later plans drop READMEs and artifacts into it. The 7 dirs are:
    - `01-ci-green/` (this plan)
    - `02-local-make-e2e/` (this plan)
    - `03-ci-red/` (Plan 11-02)
    - `04-branch-protection/` (Plan 11-04)
    - `05-installer-smoke/` (Plan 11-05)
    - `06-lic-05-deferral/` (Plan 11-03)
    - `07-hikvision-deferral/` (Plan 11-03)

    Step 3 — Write `.planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md` with this content:

    ```markdown
    # Phase 11 Evidence — v1.0 Live Environment Validation

    **Status:** in progress
    **Last updated:** {today's ISO date e.g. 2026-04-29}

    This directory holds the auditable evidence for every live-environment
    validation item Phase 11 executes. Per D-13, ALL items must show
    `Verdict: PASS` (or `Verdict: DEFERRED` with a risk-accept doc) before
    STATE.md flips milestone v1.0 to `complete`.

    ## Evidence Items

    | # | Item | Plan | Requirement | Verdict |
    |---|------|------|-------------|---------|
    | 01 | Live CI green run (Backend Coverage + Frontend Coverage + E2E Tests) | [11-01](../11-01-PLAN.md) | VALIDATE-CI-GREEN | [pending](./01-ci-green/README.md) |
    | 02 | Local `make e2e` against real dev stack | [11-01](../11-01-PLAN.md) | VALIDATE-CI-GREEN (supplemental) | [pending](./02-local-make-e2e/README.md) |
    | 03 | Live CI red regression (deliberate broken PR fails all 3 gates) | [11-02](../11-02-PLAN.md) | VALIDATE-CI-RED | [pending](./03-ci-red/README.md) |
    | 04 | Branch protection — 3 status checks required on `main` | [11-04](../11-04-PLAN.md) | VALIDATE-BRANCH-PROTECTION | [pending](./04-branch-protection/README.md) |
    | 05 | Fresh-VM installer smoke (Ubuntu 22.04 + Docker + CF tunnel + DO Functions) | [11-05](../11-05-PLAN.md) | VALIDATE-INSTALLER-SMOKE | [pending](./05-installer-smoke/README.md) |
    | 06 | LIC-05 cross-host clone test — risk-accept deferral to first prod install | [11-03](../11-03-PLAN.md) | VALIDATE-LIC-05-CLONE | [pending](./06-lic-05-deferral/README.md) |
    | 07 | Real Hikvision alertStream live test — risk-accept deferral to first prod install | [11-03](../11-03-PLAN.md) | VALIDATE-HIKVISION-LIVE | [pending](./07-hikvision-deferral/README.md) |

    ## Evidence README schema (per D-02)

    Every item README MUST have these sections in this order:

    - Date captured (YYYY-MM-DD)
    - Captured by (operator name / username)
    - Command run / action (exact command or UI step)
    - Expected (what should happen)
    - Actual (what did happen)
    - Verdict (`PASS` | `FAIL` | `DEFERRED`)
    - Artifacts (list of files in this dir)
    - External refs (GH Actions run URL, PR number — if any)

    ## Size budget (per D-03)

    Total committed evidence across this directory MUST stay under **150 MB**.
    Per-item HTML reports above 50 MB MUST be `tar.gz`-compressed before commit.
    Use `du -sh evidence/` to spot-check before each commit.

    ## Phase 11 verdict (per D-13)

    | Block | Status |
    |-------|--------|
    | All 5 active items PASS | pending |
    | Both deferral items have risk-accept docs | pending |
    | STATE.md milestone flip approved | pending |
    ```

    Replace `{today's ISO date e.g. 2026-04-29}` with the actual ISO date when running this task. Use `date +%Y-%m-%d` to obtain it.

    Step 4 — Commit immediately so later plans build on a clean shared base:

    ```
    git add .gitattributes .planning/phases/11-v1-0-live-environment-validation/evidence/
    git commit -m "docs(11-01): scaffold Phase 11 evidence dir + .gitattributes HTML rule"
    ```
  </action>
  <verify>
    <automated>cd /Users/gerswin/Proyectos/cronometrix && grep -q '^\*\.html linguist-generated=true' .gitattributes && test -f .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md && grep -q '^| 01 | Live CI green run' .planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md && for d in 01-ci-green 02-local-make-e2e 03-ci-red 04-branch-protection 05-installer-smoke 06-lic-05-deferral 07-hikvision-deferral; do test -d .planning/phases/11-v1-0-live-environment-validation/evidence/$d || { echo "MISSING dir $d"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^\*\.html linguist-generated=true' .gitattributes` exits 0
    - `.planning/phases/11-v1-0-live-environment-validation/evidence/00-overview.md` exists
    - The overview file contains all 7 row labels (01..07) AND the literal verdict-schema headings: "Date captured", "Captured by", "Command run", "Expected", "Actual", "Verdict", "Artifacts", "External refs"
    - All 7 evidence subdirectories exist with `.gitkeep` files (verifiable with the verify shell)
    - The commit `docs(11-01): scaffold Phase 11 evidence dir + .gitattributes HTML rule` exists in `git log` (verify with `git log --oneline -1`)
  </acceptance_criteria>
  <done>
    `.gitattributes` carries the HTML rule, all 7 evidence subdirectories exist with `.gitkeep`, `00-overview.md` indexes the items per the D-02 schema, and one atomic commit lands in git history. Plans 11-02/03/04/05 are now unblocked because their evidence subdirs are present.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator triggers live CI green run on GitHub Actions and pastes back run URL + job statuses</name>
  <read_first>
    - .planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md (D-11 prerequisite checklist — confirm GH Actions billing budget OK)
    - .github/workflows/ci.yml (confirm 3 job names exist VERBATIM: `Backend Coverage`, `Frontend Coverage`, `E2E Tests`)
    - CLAUDE.md § Test Coverage § Pending live validation (Phase 8 Plan 05 deferred steps A/C)
    - CLAUDE.md § End-to-End Tests § Pending live validation (Phase 9 Plan 12 deferred items 1/3)
  </read_first>
  <what-built>
    The executor (autonomous part):
    - Verifies `.github/workflows/ci.yml` has the 3 job names exactly: `Backend Coverage`, `Frontend Coverage`, `E2E Tests` (grep). If any name differs, abort with FAIL and route to `/gsd-debug` (per D-17 false-negative protocol).
    - Confirms the working branch is clean and pushed (`git status` clean, `git log @{u}..HEAD` empty).
    - Generates the operator checklist below.

    The operator (manual part):
    - Pushes `phase-11-live-validation` (or whatever the working branch name is) to GitHub.
    - Opens https://github.com/{org}/cronometrix/actions and waits for the auto-triggered workflow run to finish.
    - Records: run URL, status of each of the 3 jobs (must all be green), total duration.
    - Downloads the 3 HTML artifact bundles from the Actions run page:
      * `backend-coverage-html`
      * `frontend-coverage-html`
      * `playwright-html-report` (uploaded by E2E Tests job per Phase 9 D-12 even on success)
    - Compresses each to `.tar.gz` (per D-03 50 MB threshold) and saves them under `evidence/01-ci-green/`.
    - Captures raw job logs by clicking each job → "View raw logs" → Save as text file under `evidence/01-ci-green/{job-slug}-job.txt`.
  </what-built>
  <how-to-verify>
    Operator paste-back format (paste this back to executor when done):

    ```
    GH ACTIONS RUN URL: https://github.com/{org}/cronometrix/actions/runs/{run_id}
    BACKEND COVERAGE: {success|failure} — duration {Mm Ss}
    FRONTEND COVERAGE: {success|failure} — duration {Mm Ss}
    E2E TESTS: {success|failure} — duration {Mm Ss}
    ARTIFACTS DOWNLOADED:
      - evidence/01-ci-green/backend-coverage-html.tar.gz ({size} MB)
      - evidence/01-ci-green/frontend-coverage-html.tar.gz ({size} MB)
      - evidence/01-ci-green/playwright-report.tar.gz ({size} MB)
    JOB LOGS SAVED:
      - evidence/01-ci-green/backend-coverage-job.txt
      - evidence/01-ci-green/frontend-coverage-job.txt
      - evidence/01-ci-green/e2e-tests-job.txt
    ```

    Executor validation (run before declaring PASS):
    - All 3 statuses MUST be `success`. Any `failure` or `cancelled` → STOP, route to `/gsd-debug`, do NOT write a PASS verdict.
    - `git status` shows the 6 evidence files staged-or-tracked under `evidence/01-ci-green/`.
    - `du -sh evidence/01-ci-green/` total stays under 50 MB (within the 150 MB phase budget).
    - The run URL matches regex `https://github\.com/.*/actions/runs/[0-9]+`.

    Executor then writes `evidence/01-ci-green/README.md` per the D-02 schema with `Verdict: PASS`, lists all 6 artifacts, and pastes the run URL into the External refs section.
  </how-to-verify>
  <resume-signal>Operator pastes the format block above with all 3 jobs green and 6 artifact files saved on disk. Executor responds with the README write + commit `docs(11-01): evidence — live CI green run {run_id}`.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator runs `make e2e` locally and pastes back exit code + Playwright report</name>
  <read_first>
    - CLAUDE.md § End-to-End Tests § Local commands (the `make e2e` contract: builds backend + runs full Playwright suite)
    - frontend/playwright.config.ts (confirm webServer count and chromium project — operator's local box must have all 3 services bootable)
    - .planning/phases/11-v1-0-live-environment-validation/11-CONTEXT.md (D-11 — Rust nightly + Node 20 + Playwright chromium prereq)
  </read_first>
  <what-built>
    The executor (autonomous part):
    - Confirms operator local prereqs by asking them to run `rustup show active-toolchain && node --version && npx playwright --version` and paste the output.
    - Generates the run command + paste-back template below.

    The operator (manual part):
    - From repo root, runs:
      ```
      cd /Users/gerswin/Proyectos/cronometrix
      make e2e 2>&1 | tee .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/make-e2e.txt
      ```
    - On completion, archives the Playwright HTML report:
      ```
      tar -czf .planning/phases/11-v1-0-live-environment-validation/evidence/02-local-make-e2e/playwright-report.tar.gz -C frontend playwright-report
      ```
    - Pastes back the operator block.
  </what-built>
  <how-to-verify>
    Operator paste-back format:

    ```
    LOCAL TOOLCHAIN:
      rustup: {nightly-2026-04-01 or pinned date from rust-toolchain.toml}
      node:   v20.x.y
      playwright: 1.59.1
    MAKE E2E EXIT CODE: 0
    PLAYWRIGHT TOTAL: {N} passed, 0 failed (workers=1 fullyParallel=false expected per Phase 9)
    DURATION: {Mm Ss}
    ARTIFACTS:
      - evidence/02-local-make-e2e/make-e2e.txt ({lines} lines)
      - evidence/02-local-make-e2e/playwright-report.tar.gz ({size} MB)
    ```

    Executor validation:
    - Exit code MUST be 0. Any non-zero → STOP, route to `/gsd-debug` (D-17).
    - `make-e2e.txt` MUST contain the literal string `passed` and MUST NOT contain `failed` (excluding spec names that legitimately contain those words; check with `grep -E '^\s*[0-9]+ failed' make-e2e.txt | wc -l == 0`).
    - `playwright-report.tar.gz` exists and is under 50 MB (`du -m playwright-report.tar.gz` < 50).
    - Toolchain versions match `rust-toolchain.toml` (read it; nightly date must equal the operator-pasted version).

    Executor then writes `evidence/02-local-make-e2e/README.md` per D-02 schema with `Verdict: PASS`, lists artifacts, and includes the toolchain block in the Actual section so future re-runs know which toolchain was witnessed.
  </how-to-verify>
  <resume-signal>Operator pastes paste-back block with exit code 0 and both artifact files on disk. Executor responds with the README write + final commit `docs(11-01): evidence — local make e2e PASS` + updates `evidence/00-overview.md` row 01 verdict from `pending` to `PASS` and row 02 verdict from `pending` to `PASS`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → repo | Operator pastes external evidence (run URLs, log files); executor must verify pasted values match expected schema before treating as authoritative |
| Repo → public GitHub | Evidence dir is committed; if repo goes public, HTML reports may leak seeded test data + screenshots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Pasted GH Actions run URL | mitigate | Executor regex-validates URL matches `https://github\.com/.*/actions/runs/[0-9]+`; executor cross-checks run status by re-reading the URL via WebFetch if any field looks suspicious |
| T-11-02 | Information Disclosure | Playwright HTML reports + Vitest HTML reports committed to evidence dir | accept | Repo is currently private (D-23 / Phase 9 disclosure note). Test data uses synthetic names (Ana Pérez, Luis García) — no real PII. CLAUDE.md already documents revisit-on-public-repo |
| T-11-03 | Repudiation | Operator claims `make e2e` passed but it failed silently | mitigate | Pasted `make-e2e.txt` MUST contain literal `passed` count + zero `failed`; executor refuses to write PASS verdict if either grep fails |
| T-11-04 | Denial of Service | Evidence dir bloat exceeds 150 MB (D-03) | mitigate | Per-item executor runs `du -sh evidence/` after each commit; HTML > 50 MB triggers tar.gz compression; if total > 150 MB, escalate to user before committing |

</threat_model>

<verification>
- `.gitattributes` rule landed (grep verifies)
- 7 evidence subdirectories exist under `evidence/`
- `evidence/00-overview.md` index present with all 7 rows
- `evidence/01-ci-green/README.md` has `Verdict: PASS` + 6 artifacts listed
- `evidence/02-local-make-e2e/README.md` has `Verdict: PASS` + 2 artifacts listed
- Total `evidence/` dir size < 150 MB
- 3 atomic commits in git history matching pattern `docs(11-01):`
</verification>

<success_criteria>
- All 3 CI jobs (`Backend Coverage`, `Frontend Coverage`, `E2E Tests`) pass green on a real GitHub Actions runner with downloaded artifacts proving it
- `make e2e` exits 0 locally with all 72 Playwright specs passing
- Evidence README files conform to D-02 schema (8 required sections in fixed order)
- `evidence/00-overview.md` rows 01 + 02 transition from `pending` to `PASS`
- No code under `backend/src/`, `frontend/src/`, or `.github/workflows/` modified (D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-0-live-environment-validation/11-01-SUMMARY.md` summarizing: live run URL, local make e2e duration, artifact filenames + sizes, any anomalies (e.g. flake retries), and the next-plan handoff (11-02 deliberate red regression).
</output>
