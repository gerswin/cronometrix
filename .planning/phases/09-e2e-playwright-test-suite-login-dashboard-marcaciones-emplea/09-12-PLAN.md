---
phase: 09
plan: 12
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03, 09-04, 09-05, 09-06, 09-07, 09-08, 09-09, 09-10, 09-11]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - E2E-CI
  - E2E-CHROMIUM-ONLY
must_haves:
  truths:
    - "New job 'E2E Tests' added to .github/workflows/ci.yml — runs on push (any branch) + pull_request to main"
    - "Job pre-builds the Rust binaries (cronometrix + mock_hikvision + seed_e2e) BEFORE Playwright starts (avoids webServer.timeout cold compile)"
    - "Pinned actions: actions/checkout@v4, actions/setup-node@v4, actions/upload-artifact@v4, taiki-e/install-action@v2, Swatinem/rust-cache@v2 (parity with Phase 8 T-08-15)"
    - "permissions: contents: read at workflow level (least privilege; inherited from Phase 8)"
    - "Two artifact uploads, both if: always(), retention 14 days: playwright-report (HTML) + test-results (videos/traces/screenshots)"
    - "Job NAME is exactly 'E2E Tests' (matches CONTEXT D-15 branch-protection check name)"
    - "Hard-fail on any test failure — npx playwright test exits non-zero → job exits non-zero (no soft-warn, no override)"
    - "Phase 8 backend-coverage + frontend-coverage jobs are UNCHANGED by this plan"
    - "Branch protection step (adding E2E Tests as required) is documented as MANUAL FOLLOW-UP — mirrors Phase 8 Plan 05"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Phase 8 jobs preserved + new E2E Tests job"
      contains: "E2E Tests"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "make e2e-build"
      via: "GitHub Actions step running cargo build before Playwright"
      pattern: "cargo build --release.*--features"
---

<objective>
Add the `E2E Tests` GitHub Actions job to `.github/workflows/ci.yml`, alongside the existing Phase 8 `Backend Coverage` and `Frontend Coverage` jobs. The job pre-builds the Rust binaries, installs Playwright + chromium, runs the full E2E suite, and uploads HTML report + test-results as 14-day artifacts on every run (success or failure).

Per CONTEXT D-15 the job is intended to be a required status check on `main`. As with Phase 8 Plan 05, the actual branch-protection toggle is a manual GitHub UI step deferred to a SUMMARY checklist — this plan only ships the workflow file.

Purpose: Hard-fail E2E gate on every PR. Without this, a regression in the operator workflow can land on main.
Output: Updated `.github/workflows/ci.yml` with one new job (E2E Tests). All Phase 8 jobs untouched. Workflow is YAML-valid and the job runs to completion on a real PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-CONTEXT.md
@.planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-RESEARCH.md
@.planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-PATTERNS.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-05-SUMMARY.md
@.planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-CONTEXT.md
@CLAUDE.md
@.github/workflows/ci.yml
@Makefile

<interfaces>
<!-- Existing ci.yml structure (Phase 8 — DO NOT MODIFY): -->
- name: CI
- on: push (all branches) + pull_request (main)
- permissions: contents: read
- 2 jobs: backend-coverage + frontend-coverage
- Pinned actions: actions/checkout@v4, actions/setup-node@v4, actions/upload-artifact@v4, taiki-e/install-action@v2, Swatinem/rust-cache@v2
- 14-day artifact retention

<!-- New job structure (per RESEARCH §CI Integration): -->
```yaml
e2e-tests:
  name: E2E Tests       # MUST be this exact string for branch protection
  runs-on: ubuntu-latest
  defaults:
    run:
      working-directory: frontend
  env:
    TZ: America/Caracas
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: npm
        cache-dependency-path: frontend/package-lock.json
    - uses: actions-rust-lang/setup-rust-toolchain@v1   # OR rely on default rust if present
      with:
        toolchain: stable
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: backend
    - run: npm ci
    - name: Install Playwright Chromium
      run: npx playwright install --with-deps chromium
    - name: Build backend release binaries (cronometrix, mock_hikvision, seed_e2e)
      working-directory: backend
      run: |
        cargo build --release --bin cronometrix
        cargo build --release --bin mock_hikvision --features mock-hikvision
        cargo build --release --bin seed_e2e --features seed-e2e
    - name: Run Playwright tests
      run: npx playwright test
    - name: Upload HTML report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-html-report
        path: frontend/playwright-report
        retention-days: 14
    - name: Upload test results (videos/traces/screenshots)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-test-results
        path: frontend/test-results
        retention-days: 14
```

<!-- For Rust toolchain: re-use the same pattern Phase 8 backend-coverage used. The Phase 8 job
     pinned actions/checkout@v4, taiki-e/install-action@v2, etc., and used `rustup toolchain install nightly`
     for the coverage gate. For Phase 9 E2E, stable rustc is sufficient — no need for nightly. -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add E2E Tests job to .github/workflows/ci.yml (preserve Phase 8 jobs)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current Phase 8 layout — verify exact structure of existing 2 jobs)
    - .planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-05-PLAN.md (workflow patterns; pinned actions list)
    - .planning/phases/08-test-coverage-quality-gate-reach-90-line-coverage-and-85-bra/08-05-SUMMARY.md (T-08-15 lessons; manual follow-up pattern)
    - .planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-PATTERNS.md (§ .github/workflows/ci.yml — add E2E Tests job — full pattern)
    - .planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-RESEARCH.md (§ CI Integration; § Implementation Risks Required-status-check trap)
    - Makefile (e2e-build target — script the same steps in CI)
  </read_first>
  <action>
    1. Open `.github/workflows/ci.yml`. Confirm current structure (Phase 8 jobs: `backend-coverage`, `frontend-coverage`). DO NOT modify the existing `name`, `on`, `permissions`, or those two jobs.

    2. Append a new job `e2e-tests` AT THE END of the `jobs:` block:

       ```yaml
       e2e-tests:
         name: E2E Tests
         runs-on: ubuntu-latest
         env:
           TZ: America/Caracas
         steps:
           - uses: actions/checkout@v4
           - uses: actions/setup-node@v4
             with:
               node-version: 20
               cache: npm
               cache-dependency-path: frontend/package-lock.json
           - name: Install stable Rust toolchain
             run: |
               rustup default stable
               rustup component add clippy
               rustc --version
           - uses: Swatinem/rust-cache@v2
             with:
               workspaces: backend
           - name: Install npm deps
             working-directory: frontend
             run: npm ci
           - name: Install Playwright Chromium
             working-directory: frontend
             run: npx playwright install --with-deps chromium
           - name: Build backend release binaries (cronometrix + mock_hikvision + seed_e2e)
             working-directory: backend
             run: |
               cargo build --release --bin cronometrix
               cargo build --release --bin mock_hikvision --features mock-hikvision
               cargo build --release --bin seed_e2e --features seed-e2e
           - name: Run Playwright tests (full E2E suite)
             working-directory: frontend
             env:
               CI: 'true'
             run: npx playwright test
           - name: Upload HTML report
             if: always()
             uses: actions/upload-artifact@v4
             with:
               name: playwright-html-report
               path: frontend/playwright-report
               retention-days: 14
           - name: Upload test results (videos / traces / screenshots)
             if: always()
             uses: actions/upload-artifact@v4
             with:
               name: playwright-test-results
               path: frontend/test-results
               retention-days: 14
       ```

       Notes:
       - Job name is EXACTLY `E2E Tests` (with the space; case-sensitive). Branch protection step (Manual Follow-up) will require a status check matching this name verbatim.
       - `env: TZ: America/Caracas` at job level so any non-webServer-spawned process inherits (D-20 / RESEARCH §Critical clarification).
       - Pinned versions inherit Phase 8 conventions (T-08-15).
       - Both artifact uploads use `if: always()` per D-18 ("upload all artifacts always").
       - Retention is 14 days, matching Phase 8.

    3. Validate YAML syntax:
       ```bash
       python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('valid')"
       ```
       OR `yamllint .github/workflows/ci.yml` if available. Either should succeed.

    4. Confirm Phase 8 jobs are unchanged:
       ```bash
       grep -c "name: Backend Coverage" .github/workflows/ci.yml | grep -q "^1$"
       grep -c "name: Frontend Coverage" .github/workflows/ci.yml | grep -q "^1$"
       grep -c "name: E2E Tests" .github/workflows/ci.yml | grep -q "^1$"
       ```
       All three should output 1.

    5. Confirm pinned actions parity with Phase 8 (T-08-15):
       ```bash
       grep -c "actions/checkout@v4" .github/workflows/ci.yml | grep -qE "^[3-9]"
       grep -c "actions/setup-node@v4" .github/workflows/ci.yml | grep -qE "^[2-9]"
       grep -c "actions/upload-artifact@v4" .github/workflows/ci.yml | grep -qE "^[3-9]"
       grep -c "Swatinem/rust-cache@v2" .github/workflows/ci.yml | grep -qE "^[2-9]"
       ```
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('OK')" 2>&1 | grep -q "OK" && grep -c "name: Backend Coverage" .github/workflows/ci.yml | grep -q "^1$" && grep -c "name: Frontend Coverage" .github/workflows/ci.yml | grep -q "^1$" && grep -c "name: E2E Tests" .github/workflows/ci.yml | grep -q "^1$" && grep -q "TZ: America/Caracas" .github/workflows/ci.yml && grep -q "playwright-html-report" .github/workflows/ci.yml && grep -q "playwright-test-results" .github/workflows/ci.yml && grep -q "retention-days: 14" .github/workflows/ci.yml && grep -q "permissions:" .github/workflows/ci.yml && grep -q "contents: read" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` is valid YAML (parses with PyYAML)
    - Job `e2e-tests` exists with `name: E2E Tests` (exact string)
    - 3 jobs total: Backend Coverage, Frontend Coverage, E2E Tests
    - Existing Phase 8 jobs are byte-identical (only new content was appended)
    - `permissions: contents: read` is at workflow scope (preserved from Phase 8)
    - Job uses pinned actions: actions/checkout@v4, actions/setup-node@v4, actions/upload-artifact@v4, Swatinem/rust-cache@v2 (T-08-15 parity)
    - Both artifact uploads have `if: always()` AND `retention-days: 14`
    - Backend pre-build step builds 3 binaries: cronometrix, mock_hikvision (with feature), seed_e2e (with feature)
    - Workflow runs `npx playwright test` (full suite, exits non-zero on any test failure)
  </acceptance_criteria>
  <done>E2E Tests job lands; YAML valid; Phase 8 jobs untouched; pinned actions enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ secrets | The job does NOT use secrets — all keys are e2e test placeholders (per Plan 01 webServer.env) |
| Pinned actions ↔ supply chain | Phase 8 T-08-15 mandates pinned actions; this plan inherits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering / Supply chain | Unpinned GitHub Actions | mitigate | All 4 actions pinned to major version, matching Phase 8 conventions; new entries reviewed in this plan |
| T-09-06 | Tampering | Cargo lockfile drift causing flaky CI | mitigate | Swatinem/rust-cache@v2 caches `target/` + `~/.cargo/registry`; lockfile committed |
| T-09-07 | Information Disclosure | Artifact uploads contain test screenshots | accept | Test data is synthetic seed_e2e; repo is private (per CLAUDE.md note); 14-day retention bounded |
| T-09-trap-1 | Operational | Branch protection added before workflow proven green | accept (deferred manual step) | Mirror Phase 8 Plan 05 — branch protection is documented in SUMMARY as Manual Follow-up; workflow must run green on a real PR FIRST |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
- All 3 job names present in the file
- No `actions/*@vN.M.O` (over-pinned) or `actions/*@<sha>` (under-pinned vs Phase 8 convention) — only `@vN` major-version pins
- `if: always()` on both upload-artifact steps
- The workflow is committed but NOT yet validated by a real GitHub run — that's the Manual Follow-up
</verification>

<success_criteria>
- E2E Tests job lands in ci.yml with correct name + structure
- Phase 8 jobs preserved verbatim
- Pinned actions inherit Phase 8 T-08-15
- Hard-fail philosophy: any test failure → job exits non-zero → PR blocks (after branch protection added)
- Manual Follow-up documented in 09-12-SUMMARY.md (positive run + first-PR validation + branch protection toggle)
</success_criteria>

<output>
Create `.planning/phases/09-e2e-playwright-test-suite-login-dashboard-marcaciones-emplea/09-12-SUMMARY.md` with a "Manual Follow-up" section mirroring Phase 8 Plan 05 format:

  **Manual Follow-up (deferred — requires live GitHub run):**
  1. Open a PR with the workflow change; confirm `E2E Tests` job runs to completion green on GitHub Actions
  2. Open a deliberate red PR (break a spec); confirm `E2E Tests` FAILS and the artifacts are downloadable
  3. In GitHub UI: Settings → Branches → branch protection rule for `main` → Require status checks → add `E2E Tests` to required list

Phase 9 is NOT considered fully active in production CI until items 1-3 all complete. Mirror the Phase 8 ethos.
</output>
