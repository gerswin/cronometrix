# Rust build artifacts
backend/target/

# Environment files
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Node (for frontend)
node_modules/
.next/

# IDE
.idea/
.vscode/

# Pencil design files (large binary)
*.pen

# Generated design exports
images/

# Phase 9: Playwright E2E artifacts (regenerated per run; never commit)
frontend/e2e/.auth/
frontend/playwright-report/
frontend/test-results/

# Backend runtime artifacts (local SQLite, libSQL replica WAL, llvm-cov profraw, dev data)
backend/data/
backend/cronometrix.db
backend/cronometrix.db-*
backend/*.profraw
backend/default_*.profraw

# Dokku deploy secrets
deploy/dokku/deploy.env

# Demo deploy local SQLite data (generated, contains seeded credentials)
deploy/data/

# Backend load-test outputs (regenerated per run)
backend/load-test-results/

# Claude Code per-user config (local settings, hooks, worktrees)
.claude/
.agents/

# Kiro IDE config (per-user)
.kiro/

# Skill manager lockfile (regenerated)
skills-lock.json
