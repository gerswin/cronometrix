meta {
  name: setup status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/setup/status
  body: none
  auth: none
}

tests {
  test("returns initialized flag", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("initialized");
  });
}
