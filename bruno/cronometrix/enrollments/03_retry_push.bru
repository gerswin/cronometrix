meta {
  name: retry push
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/enrollments/{{sample_enrollment_id}}/pushes/{{sample_device_id}}/retry
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
