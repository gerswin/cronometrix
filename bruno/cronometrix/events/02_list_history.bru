meta {
  name: list events (history)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/events?limit=20
  body: none
  auth: bearer
}

params:query {
  limit: 20
}

auth:bearer {
  token: {{access_token}}
}
